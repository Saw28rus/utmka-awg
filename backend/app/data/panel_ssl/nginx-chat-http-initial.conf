# Чат-домен, до выпуска сертификата: только ACME, всё остальное 404
server {
    listen 80;
    listen [::]:80;
    server_name {{DOMAIN}};

    location /.well-known/acme-challenge/ {
        root {{WEBROOT}};
        allow all;
    }

    location / {
        return 404;
    }
}
