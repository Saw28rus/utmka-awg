# HTTP: ACME + редирект на HTTPS (финальная конфигурация)
server {
    listen 80;
    listen [::]:80;
    server_name {{DOMAIN}};

    location /.well-known/acme-challenge/ {
        root {{WEBROOT}};
        allow all;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
